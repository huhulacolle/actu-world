import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.actuworld.fr',
  appName: 'ActuWorld',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
