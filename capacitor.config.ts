import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.actuworld.com',
  appName: 'ActuWorld',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
